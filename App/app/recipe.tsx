import { ScrollView } from "react-native";
import AppText from "@/components/global/app-text";

const Recipe = () => {
    return (
        <ScrollView>
            <AppText text={'test text'}></AppText>
        </ScrollView>
    )
}

export default Recipe;